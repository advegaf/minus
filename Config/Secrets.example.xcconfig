// Copy to Secrets.xcconfig (gitignored) and fill in your values.
//
// Local signing team (so device builds sign without editing project.yml).
DEVELOPMENT_TEAM =

// Your registered App ID (the repo ships only the neutral example value).
MINUS_BUNDLE_ID = com.example.minus
