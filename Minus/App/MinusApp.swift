import SwiftUI

@main
struct MinusApp: App {
    var body: some Scene {
        WindowGroup {
            Color(red: 16 / 255, green: 16 / 255, blue: 16 / 255)
                .ignoresSafeArea()
                .preferredColorScheme(.dark)
        }
    }
}
